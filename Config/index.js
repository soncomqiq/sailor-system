const database = `boatrental`;
const db_host = `localhost`;
const username = `root`;
const password = `12345678`;

module.exports = {
    database: database,
    host: db_host,
    user: username,
    pass: password,
}